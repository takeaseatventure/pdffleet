{
  "name": "pdffleet-api",
  "version": "1.0.0",
  "description": "PDF generation API — render HTML/URL to PDF via headless Chromium. Self-hosted, undercutting APITemplate/PDFMonkey/Paperplane.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node test/run.js",
    "install-browser": "playwright install chromium --with-deps"
  },
  "dependencies": {
    "playwright": "^1.48.0"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
